pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Picture2PC"

include(":desktop")
include(":common")
include(":android")